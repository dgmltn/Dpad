# :ui

All Dpad screens (remote, devices/pairing, shortcuts) + their ViewModels — the tiny-app single-UI-module collapse. Depends on `:design` and `:domain`. ViewModels consume `:domain` contracts via Koin; never touches `:data`/`:protocol`.
